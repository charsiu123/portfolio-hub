# Habit PWA smoke test

Serve the hub and open `/demos/habits/`. Confirm the page loads, a habit can be added and started, and DevTools reports `sw.js` controlling the `/demos/habits/` scope. Reload once to confirm the service worker remains registered.
